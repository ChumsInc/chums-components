import styled from "styled-components";

const StyledTabList = styled.ul`
    &.nav {
        .nav-item {
            .nav-link {
                .btn-close {
                    width: 0.75rem;
                    height: 0.75rem;
                    margin-left: 0.25rem;
                    line-height: 0.75rem;
                    font-size: 0.75rem;
                }
            }
        }
    }
`;

export default StyledTabList;
