/// <reference types="react" />
import { AlertProps } from "./Alert.types";
declare const Alert: ({ message, color, title, className, context, count, canDismiss, onDismiss, children }: AlertProps) => JSX.Element;
export default Alert;
